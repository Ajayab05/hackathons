apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: 422060263884.dkr.ecr.ap-south-1.amazonaws.com/backend-app:IMAGE_TAG
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
